#include <stdio.h>
int numeros[100], i;
int main()
{
    for(i = 0; i < 100; i++)
    {
        numeros[i] = 2 * (i + 1);
    }
    printf("Los 100 primeros numeros pares son:\n");
    for(i = 0; i < 100; i++)
    {
        printf("%d ", numeros[i]);
    }
    return 0;
}
